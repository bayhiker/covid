/*
 * YouQuizTopBar Messages
 *
 * This contains all the text for the YouQuizTopBar component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.YouQuizTopBar';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Problems',
  },
});
