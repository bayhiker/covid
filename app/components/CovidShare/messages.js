/*
 * CovidShare Messages
 *
 * This contains all the text for the CovidShare component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CovidShare';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CovidShare component!',
  },
});
