/*
 * CovidTable Messages
 *
 * This contains all the text for the CovidTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CovidTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CovidTable component!',
  },
});
