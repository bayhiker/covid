// import { selectProblemsPageDomain } from '../selectors';

describe('selectProblemsPageDomain', () => {
  it.skip('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
